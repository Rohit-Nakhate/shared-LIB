def call(Map config = [:]) {
  echo"------------------------ executing config file ----------------------------------------------------"
  echo "-----------------------< HI I AM ${config.name} >-------------------------------------------------"
}
